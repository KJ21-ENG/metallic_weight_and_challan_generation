# Local Print Service Configuration
PORT=3001

# Optional: Custom media size for labels (in mm)
LABEL_WIDTH_MM=75
LABEL_HEIGHT_MM=125

# Optional: Print quality (DPI)
PRINT_DPI=300

# Optional: Default printer names (if you want to set defaults)
DEFAULT_LABEL_PRINTER=
DEFAULT_CHALLAN_PRINTER=
