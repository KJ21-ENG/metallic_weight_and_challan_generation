# Server configuration
PORT=4000
# Connection string format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://postgres:postgres@localhost:5432/metallic_challan
# Optional: override project root used to save PDFs. Defaults to repo root.
PROJECT_ROOT=
